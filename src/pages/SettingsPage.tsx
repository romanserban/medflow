import { PagePanel } from '../components/PagePanel'

export function SettingsPage() {
  return (
    <PagePanel eyebrow="Workspace" title="Settings">
      Settings can be expanded when user, facility, and permission requirements are ready.
    </PagePanel>
  )
}
